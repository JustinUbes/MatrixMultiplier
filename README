These files support the POSIX implementation of the thread pool project.

Source files:

- client.c (client program that uses thread pool)

- threadpool.c (implementation of thread pool)

- threadpool.h (header file containing function prototypes)

Makefile

- To run the program, change to the directory which you have this file in. 
- Type "make"
- Assuming this directory is in your path, type "example yourmatrix1.txt yourmatrix2.txt"
    - If not in path run (export PATH=/your/directory:$PATH)
- If the matrices you provided are able to be multiplied, you will see...
    - How many rows and columns they contain 
    - what each thread adds to an index of the product matrix.
